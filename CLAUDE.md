# run-advisor

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `constantin-me/run-advisor`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
